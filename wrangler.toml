# Cloudflare Pages — static output from `npm run build`.
# Dashboard: build command `npm run build`, output directory `dist`.
name = "vanshkmehta-dev"
compatibility_date = "2026-09-15"
pages_build_output_dir = "./dist"
